devnet-in-a-box a0e5765
This file exists to satisfy the Railway watch-path filter (/packages/explorer/**)
so deploys that only touch docker/ are not skipped. Content is irrelevant; bump it
to force a deploy.
